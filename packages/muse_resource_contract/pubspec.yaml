name: muse_resource_contract
description: Typed Dart projection of the Muse resource, presentation, and engine-session wire contracts.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

