name: muse_web_viewer_surface
description: Certified read-only open-file-viewer adapter and isolated bridge runtime for Muse.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  muse_engine_adapter:
    path: ../muse_engine_adapter
  muse_resource_bridge:
    path: ../muse_resource_bridge
  muse_resource_contract:
    path: ../muse_resource_contract

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

